package com.example.musicplayer.model

import androidx.lifecycle.ViewModel
import com.example.musicplayer.database.FavoriteDatabase

class CartViewModel(private val favoriteDatabase: FavoriteDatabase) : ViewModel() {
//    fun updateProduct(favoriteList: FavoriteList) =
//        favoriteDatabase.updateProduct(favoriteList)
}